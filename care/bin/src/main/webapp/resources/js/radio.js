function getGender(event) {
  document.getElementById('sex').value = 
    event.target.value;
}

/* function fnRadioName(){
        var radioId = $('input[name="gener"]:checked').val();
        var rasioNm = $("label[for='"+radioId+"']").text(); // 라벨값을 불러온다.
        alert(rasioNm);
        rasioNm.add
    } */